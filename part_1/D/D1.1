-- Απενεργοποιούμε αυτό το setting της postgres για να μην χρησιμοποιεί
-- τον δικό της αλγόριθμο sequence sqan στα queries που τρέχουμε.


SET enable_seqscan = off;


-- Δημιουργούμε το index για το πρώτο ερώτημα χρησιμοποιώντας το b-tree
-- ώστε να επιταχύνουμε τα αποτελέσματα από τη χρήση του GROUP BY.
-- Επιπλέουν μπορούμε να αναδιατάξουμε τα στοιχεία του δέντρου όπως θέλουμε
-- και να βάλουμε στοιχεία null στο τέλος.


CREATE INDEX
    query_1
ON
    accident_information
USING
    BTREE(first_road_class ASC NULLS LAST);


-- Σχετικές σελίδες:
-- https://www.postgresql.org/docs/current/indexes-ordering.html
